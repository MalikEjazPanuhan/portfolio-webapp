services:
  - type: web
    name: portfolio-api
    runtime: node
    plan: free
    buildCommand: |
      npm install
      apt-get update
      apt-get install -y ffmpeg python3-pip
      pip3 install yt-dlp
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 5001
        